/**
 * BannerAdWrapper — STUB until real AdMob IDs are configured.
 *
 * The react-native-google-mobile-ads SDK has been removed because it crashes
 * at native init with placeholder IDs (GADInvalidInitializationException).
 * This component returns null until the SDK is re-added with real IDs.
 *
 * PLAT-011 (EPIC-05)
 */
export function BannerAdWrapper() {
  return null;
}
